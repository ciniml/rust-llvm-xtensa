LEVEL = ../../../..
LIBRARYNAME = LLVMXtensaAsmParser

# Hack: we need to include 'main' Xtensa target directory to grab private headers
CPP.Flags += -I$(PROJ_OBJ_DIR)/.. -I$(PROJ_SRC_DIR)/..

include $(LEVEL)/Makefile.common
